export type Stat = {
  label: string;
  value: string;
};

export type Feature = {
  icon: string;
  title: string;
  description: string;
};

export type Benefit = {
  icon: string;
  title: string;
  description: string;
};

export type Integration = {
  icon: string;
  name: string;
};